import { DEVICE, DIMENSION } from "helper/Consts";

export default {
    container: {},
    content: {
        height: DEVICE.DEVICE_HEIGHT - DIMENSION.HEADER_HEIGHT * 1.5,
        paddingBottom: 0
    }
};
